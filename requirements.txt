streamlit>=1.49
pandas>=2.0
bcrypt>=4.0

# Neon Postgres. Without it the app falls back to SQLite in data/portal.db.
psycopg[binary]>=3.1

# Optional: only needed for the Push to Sheets button.
gspread>=6.0
